specVersion: "v1"
serviceId: "report-generator"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "report-generator-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
